use crate::Node;

pub struct Lexicon {
    todo: String,
}

impl Lexicon {
    pub fn new(lexicon: &str) -> Self {
        todo!();
    }

    pub fn search(&self, query: &str, offset: usize) -> Option<Vec<Node>> {
        todo!();
    }
}
